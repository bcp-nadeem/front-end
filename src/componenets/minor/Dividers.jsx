import Divider from '@mui/material/Divider';

const Dividers = ({variant, component, orientation}) => {
  return (
    <Divider variant={variant} component={component} orientation={orientation} />
  )
}

export default Dividers